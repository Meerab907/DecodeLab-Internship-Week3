# DecodeLab-Internship-Week3
I built a REST API using Node.js, Express, Sequelize, and SQLite with full CRUD operations for users. It includes secure authentication using bcrypt for password hashing and JWT for login sessions. Users can register, log in, and access a protected profile route using token-based authentication verified through middleware.
